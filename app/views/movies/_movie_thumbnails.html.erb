<% if movie.external_fanart %>
  <img class="video-thumbnail" src="<%= movie.external_fanart %>">
<% else %>
  <% movie.thumbnails.each do |thumb| %>
    <img class="video-thumbnail hide" src="<%= thumb.url %>" alt="">
  <% end %>
<% end %>
