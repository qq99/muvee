<%= render 'subnav' %>

<%= render 'remote_actions' %>
<div id="tile-list" class="tile-list" define="{pager: new Muvee.Pager(this)}" refresh="movies-index-panel">
  <% while @movies.size > 0 %>
    <%= layout(@movies) %>
  <% end %>
</div>
