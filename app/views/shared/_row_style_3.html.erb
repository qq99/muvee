<div class='tile-row'>
  <div class='col-1-3 flex-down'>
    <% next_objects = objects.shift(2) %>
    <% next_objects.each do |object| %>
      <%= render 'shared/tile', object: object %>
    <% end %>
  </div>
  <div class='col-1-3 flex-down'>
    <% next_objects = objects.shift(2) %>
    <% next_objects.each do |object| %>
      <%= render 'shared/tile', object: object %>
    <% end %>
  </div>
  <div class='col-1-3 flex-down'>
    <% next_objects = objects.shift(2) %>
    <% next_objects.each do |object| %>
      <%= render 'shared/tile', object: object %>
    <% end %>
  </div>
</div>
